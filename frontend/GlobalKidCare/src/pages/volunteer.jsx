import { StarsCanvas } from "../canvas";

const Volunteer = () => {
  return (
    <div className="relative w-full h-screen z-0 bg-primary flex items-center justify-center">
      Volunteers Page Coming Soon... 
      <StarsCanvas />
    </div>
  );
};

export default Volunteer;
